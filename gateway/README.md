# SoundCanvas GraphQL Gateway

This service will expose a GraphQL API to the frontend.

Planned responsibilities:
- Issue signed URLs for image upload (S3).
- Store image + sound metadata.
- Call the C++ service to generate sound.
